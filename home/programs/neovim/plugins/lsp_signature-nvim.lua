require("lsp_signature").setup({
  auto_close_after = 0.5,
  transparency = 80,
  timer_interval = 100,
  toggle_key = [[<M-x>]],
  select_signature_key = [[<M-n>]],
  move_cursor_key = [[<M-p>]],
})
