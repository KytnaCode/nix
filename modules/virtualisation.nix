{pkgs, ...}: {
  virtualisation = {
    podman.enable = true;

    virtualbox.host.enable = true;

    libvirtd = {
      enable = true;
      qemu = {
        package = pkgs.qemu_kvm;
        runAsRoot = true;
        swtpm.enable = true;
      };
    };
  };
}
